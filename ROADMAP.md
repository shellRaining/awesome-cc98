# Awesome CC98 路线图

Awesome CC98 是一个独立仓库，先维护可核验的 CC98 客户端目录，再以同一份结构化数据建设可探索的网页 2D 博物馆。路线图只描述阶段目标；实时进度见 [项目状态](docs/status.md)，已经确定的数据和素材边界见 [项目决策](docs/project-decisions.md)。

## 总体目标

- 收集 CC98 官方与第三方客户端、替代界面、扩展和历史项目，并让名称、作者、状态、许可证与链接均可追溯。
- 以 YAML 维护策展数据，通过校验后生成 JSON，供 README、普通网页画廊和游戏共同消费。
- 建设一个浏览器中的 2D 博物馆，让访客操控角色探索展厅、查看作品、触发展示交互和彩蛋。
- 对网络下载、作者提供和生成的素材统一记录来源、作者、授权、署名、隐私复核与文件哈希。

## M0：目录基础设施（已完成）

交付内容：

- 建立独立仓库及代码、数据和第三方素材的许可证边界。
- 定义 `exhibits/`、`collections/`、`ASSETS.yml` 和 `generated/` 的职责。
- 建立 JSON Schema、YAML 校验、README/JSON 生成和自动化测试。
- 明确收录、事实核验、素材权利与隐私规则。

完成标准：全新检出后能够安装依赖并运行 `pnpm check`；生成文件可复现；无效引用、非法 YAML 和不合规素材记录会被拒绝。

## M1：可用的策展目录（已完成基线）

交付内容：

- 建立首批 24 个展品记录，覆盖当前客户端、替代界面、实验项目、官方参考和历史项目。
- 建立 README 与画廊两套策展集合，集合只保存选择、分组和顺序，不复制展品事实。
- 为后续图标、截图、视频和宣传资料建立候选素材清单。
- 生成公开 README 和供运行时消费的 `generated/catalog.json`。

完成标准：所有展品至少有可核验来源和核验日期；分类、生命周期和许可证不依赖猜测；集合引用有效；README 与目录数据一致。

M1 完成表示目录基线可用，不表示内容冻结。新增作品、链接失效、状态变化和素材补充继续作为日常策展工作维护。

## M1A：素材入库与展陈准备（进行中）

这一工作流为 M2 准备作者头像、项目图标、海报、截图、视频，以及博物馆共用的角色、家具、贴图和音效。

交付内容：

- 下载已经确认可以使用的 CC98/GitHub 作者头像，并保留原位置、作者和署名。
- 逐项审核项目图标、截图和宣传资料；必要时取得许可、裁切脱敏或使用模拟数据重制。
- 将通过审核的文件登记为本地素材，填写媒体类型、来源、获取日期、权利依据、署名、隐私结果和 SHA-256。
- 为自制或生成素材保留提示词、工具、编辑过程和发布条款。

完成标准：仓库中不存在未登记的发布素材；每个本地文件均可追溯且权利状态允许再分发；候选素材不会被网站或游戏读取；人工复核通过后 `pnpm check` 成功。

## M2：2D 博物馆纵向切片（待开始）

目标是先做出一个内容不多但端到端可玩的网页版本，用它验证数据、素材和交互架构。

交付内容：

- 完成浏览器游戏引擎与前端集成方式的技术验证，并记录选型依据。
- 定义 `scenes/*.yaml` 及其 Schema，只保存房间、坐标、展品 ID 和交互配置。
- 实现一个大厅或主题展厅，包括角色移动、镜头、碰撞和基础场景切换。
- 接入生成后的 JSON，放置 3–5 个代表性展品。
- 实现展牌、放大查看、来源/署名面板，以及至少一种作品专属交互和彩蛋。
- 建立基础加载、错误提示、静音和键盘操作体验。

完成标准：访客打开网页即可完成“进入展厅—移动探索—发现展品—查看详情—触发交互”的完整流程；运行时不直接读取 YAML 或未批准候选素材；所有可见第三方素材均能查看来源和署名。

## M3：完整博物馆（待开始）

交付内容：

- 按年代、平台或交互形态扩充多个主题展厅，覆盖现有策展集合。
- 为客户端补充图标、截图、海报、演示或可交互的界面还原。
- 增加导览、隐藏路线、收藏品、作品间关联和彩蛋系统。
- 补充移动端操作、手柄支持、可访问性、音频控制和本地进度保存。
- 把场景与展品解耦，使新增展品无需修改游戏核心代码。

完成标准：主要策展集合均可在博物馆中访问；刷新或版本升级不会破坏进度；低性能设备可以降级运行；展品事实、场景配置和素材来源保持单一可信来源。

## M4：发布与社区维护（待开始）

交付内容：

- 完成性能、兼容性、可访问性、链接、权利和隐私审计。
- 建立预览部署、正式发布、错误监控和数据迁移流程。
- 为展品、素材、展厅与彩蛋贡献提供模板和可验证的评审清单。
- 发布版本说明、策展说明和完整署名清单。

完成标准：发布构建可复现；自动检查与人工资产审查均有记录；贡献者可以只修改对应的数据或场景文件完成常见贡献。

## 当前执行顺序

1. 完成本轮作者头像与可用项目素材的登记、哈希和人工复核。
2. 补上“仓库文件必须有登记记录”的自动检查，清理重复或占位素材。
3. 对候选游戏引擎做最小技术验证，确定 M2 的项目结构。
4. 定义场景 Schema，制作单展厅纵向切片。
5. 验证交互与素材流水线后，再批量扩充展厅和作品内容。
