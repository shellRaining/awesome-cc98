# 参与 Awesome CC98

这里接受新项目、资料纠错、状态更新和素材补充。每条可验证的事实都要指向公开来源；不确定的内容可以保留，但需要写清楚不确定在哪里。

## 开始前

需要 Node.js 22 或更高版本，以及 pnpm 11.6：

```bash
pnpm install
pnpm check
```

`pnpm check` 会校验 YAML、项目间引用、素材权利和 README 生成结果，并运行测试。README 由生成器维护，不要直接修改。

## 投稿一个项目

1. 复制一个现有的 `exhibits/<id>/exhibit.yaml`，目录 ID 使用稳定的小写短横线命名。
2. 填写项目名称、形态、平台、生命周期、链接和来源。所有日期写成带引号的 ISO 日期，如 `"2026-07-29"`。
3. 为作者、平台、功能、链接、许可证和项目关系填写 `source_refs`。找不到许可证时写 `status: unknown`，不要把“没找到”写成“闭源”。
4. 把项目 ID 加入 `collections/readme.yaml` 的合适分区。
5. 运行 `pnpm generate` 和 `pnpm check`，提交 YAML、生成后的 README 和必要素材。

事实来源优先使用项目仓库、Release、应用商店、包注册表、作者宣传帖和官方指南。聚合站、转载和搜索摘要只能作为线索，不能单独支撑项目状态、作者或许可证判断。

## 平台和状态

`platforms.claimed` 记录作者或源码声明的目标平台，`platforms.released` 只记录已有安装包、商店页或在线服务的平台。比如项目 README 写了六个平台，但 Release 只有 Android APK，两个字段不能填成相同内容。

状态定义和分区规则见 [docs/criteria.md](docs/criteria.md)。Star 数不作为准入条件。

## 图片与演示

不要提交包含真实帖子、用户名、头像、私信、IP、访问令牌或可追溯水印的图片。客户端截图需要作者授权，或者使用 mock 数据自行构建。开源代码许可证通常不包含图标、商标和截图的使用权。

每个准备公开的素材都要记录来源、SHA-256、替代文本和权利状态。完整规则见 [docs/assets.md](docs/assets.md)。尚未取得授权时可以先提交纯文字展品。

## Pull Request

PR 需要说明新增或修改了哪些展品、使用了哪些一手来源，以及仍有哪些字段待核实。涉及素材时一并说明授权或许可证依据。请勿在 Issue、PR、提交历史或附件中放入账号凭据和未脱敏的论坛内容。
